/**
 * Synapse façade — strategy: standalone.
 * Source: `components/synapse/stepper/design-spec.md`
 * Load `components/synapse-theme.css` only (not `ids-theme.css`).
 */
export {
  SynapseStepper,
  type SynapseStepperProps,
  type SynapseStepperOrientation,
} from "./SynapseStepper";
