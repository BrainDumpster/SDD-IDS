/**
 * Synapse façade — strategy: standalone.
 * Source: `components/synapse/errorcard/design-spec.md`
 * Load `components/synapse-theme.css` only (not `ids-theme.css`).
 */
export {
  SynapseErrorCard,
  type SynapseErrorCardProps,
  type SynapseErrorCardAction,
} from "./SynapseErrorCard";
