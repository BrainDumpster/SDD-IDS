## Metadata

Content for Metadata.

Use `var(--color-text-primary)`.

## Layout & Measurements

Content for Layout & Measurements.

Use `var(--color-text-primary)`.

### Slot geometry (Figma-verified)

| Slot | Property | Value | Node |
| --- | --- | --- | --- |
| Container | border-radius | `var(--radius-md)` | stub-main / get_variable_defs |

## Tokens

Content for Tokens.

Use `var(--color-text-primary)`.

## States (Light Theme)

| State | Background | Border | Text/Icon |
| --- | --- | --- | --- |
| default | `var(--color-bg-primary)` | `var(--color-border)` | `var(--color-text-primary)` |

## States (Dark Theme)

Content for States (Dark Theme).

Use `var(--color-text-primary)`.

## Interactions

Content for Interactions.

Use `var(--color-text-primary)`.

## Composition & API (runtime)

Content for Composition & API (runtime).

Use `var(--color-text-primary)`.

## Codegen Contract (Framework-Agnostic Blueprint)

### Slot / anatomy schema
- root, label

### Variant / option matrix
- default

### Accessibility
- role + aria

### Fallback / error rules
- unknown variant → default

### Validation checklist
- [ ] sections present

## Source Mapping

- file key: stubFileKeyABCDEFGH
- node id: 1:2
- verification method: Figma REST API
