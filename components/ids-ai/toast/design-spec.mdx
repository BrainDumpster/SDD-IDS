<!-- ds:inherits root-spec -->
# Toast (IDS)

## Metadata
- Component: Toast
- Category: Alerts and Notifications
- Design system: IDS-AI
- File key: `rr8F0NGA6RNY9Z0cNeh3jh`
- Component node: `10823:74993` (main component **symbol**; published frame size **`617×48`** with link + close in default instance)
- Scenarios node: `43339:4503` (frame **`content`** — grid of Toast **instances**: five at **`516×48`**, five at **`617×48`**)
- Element node: `44506:257602` (frame **`.Toast icons`** — `16×16` severity icon **symbols**: Info, Success, Warning, Major, Critical; maps to runtime `shapeName` / `type` table below)
- Component URL: https://www.figma.com/design/rr8F0NGA6RNY9Z0cNeh3jh/IDS-AI-Design-Library--Test-?node-id=10823-74993&m=dev
- Scenarios URL: https://www.figma.com/design/rr8F0NGA6RNY9Z0cNeh3jh/IDS-AI-Design-Library--Test-?node-id=43339-4503&m=dev
- Element URL: https://www.figma.com/design/rr8F0NGA6RNY9Z0cNeh3jh/IDS-AI-Design-Library--Test-?node-id=44506-257602&m=dev
- **Last live verification (2026-05-10):** Figma MCP `get_variable_defs` on `10823:74993` — bindings include `Corner Radius/radius-8`, `Spacing/space-32` / `space-24` / `space-8`, `Padding/padding-24` / `padding-14` / `padding-2`, `Color/Static/Gray/900`, `Color/Border/White`, `Body 2`, `Sizing/size-16` / `size-12`.

### Figma structure reference (validated nodes)
| Role | Node id | Notes |
| --- | --- | --- |
| Toast root (symbol) | `10823:74993` | Auto-layout horizontal; `gap` = `Spacing/space-32` between **Content** and **Action Container** |
| Content (icon + message) | `39484:7445` | Auto-layout row; `gap` = `space-8`; dev export uses **vertical alignment start** (`items-start`) for the row |
| Icon column wrapper | `43339:4093` | `padding-y` = `padding-2`; wraps `16×16` icon instance |
| Message | `39484:7448` | Body 2 — `Color/Static/Gray/White`, 14/20 |
| Action Container | `10823:75000` | `gap` = `space-24` between optional **Link** and **Close** |
| Link | `13607:248619` | Body 2 underline — `Color/Text/White` |
| Close control | `10823:75002` | `12×12` — **shape-x** asset |

## Layout & Measurements
- Item container: `height: 48px`, `padding-inline: 24px`, `padding-block: 14px`.
- Item sample widths from Figma: `516px` (without link), `617px` (with link); runtime width is container-driven.
- Root surface: `background: var(--color-static-gray-900)`, `border: 1px solid var(--color-border-white)`, **`border-radius: var(--corner-radius-radius-8)`** (validated on component `10823:74993`; Figma variable `Corner Radius/radius-8`).
- Row composition: horizontal flex, `align-items: center`, **`gap: var(--spacing-space-32)`** between:
  - `ContentGroup` (status icon + message)
  - `ActionGroup` (optional link + close)  
  Wider hosts may additionally pin the action group to the trailing edge (`margin-inline-start: auto` on `ActionGroup`) if product layout requires it; the Figma component frame uses the fixed `32px` inter-group gap.
- Content row: horizontal layout with icon/message gap **`var(--spacing-space-8)`** (Figma `Spacing/space-8`).
- **Vertical alignment (Figma vs runtime):** On node `10823:74993`, the **Content** frame (`39484:7445`) is auto-layout with **start** cross-axis alignment in the exported structure; icon column uses `padding-block: var(--padding-padding-2)` so the **16×16** glyph optically aligns with Body 2. Implementations **may** use `align-items: center` on the content row for single-line toasts when product QA matches Figma screenshots.
- Status icon column (Figma): `padding-block: var(--padding-padding-2)` around the `16×16` icon slot (`Padding/padding-2`).
- Action row: horizontal layout with link/close gap **`var(--spacing-space-24)`** when link exists (Figma `Spacing/space-24`).
- Status icon slot: fixed `16x16` container and `16x16` rendered icon (no scaling above slot size).
- Close icon slot: `12x12` using `shape-x`.
- Demo message: `This is a temporary and brief notification following a user action.` (matches Figma copy on `10823:74993`; Storybook demos use the `message` prop).
- Demo link label: `View Details` (matches Figma link on `10823:74993`; Storybook demos use the `linkLabel` / link contract).

## Tokens

### Shape (root chrome)
- **Border radius:** `var(--corner-radius-radius-8)` (Figma `Corner Radius/radius-8`; must appear on Toast root in implementation).

### Core surface/text tokens
- Surface: `var(--color-static-gray-900)`
- Border: `var(--color-border-white)`
- Message text: `var(--color-static-gray-white)`
- Link text: `var(--color-text-white)`
- Close icon: `var(--color-icon-white)`

### Status tokens and icon mapping
Shell outline is **not** severity-colored on the validated component (`10823:74993`): stroke uses **`var(--color-border-white)`** (Figma variable collection: `Color/Border/White`). Severity is communicated by the **status icon** only.

Figma icon reference frame `44506:257602` labels severities **Info, Success, Warning, Major, Critical** (`16×16` symbols). Map **Warning** → runtime `minor-warning` + `status-warn-tri-solid` per product naming.

| Type | Icon (`shapeName`) | Icon color token | Message text |
|---|---|---|---|
| `info` | `info-circ-solid` | `var(--color-icon-alerting-info)` | `var(--color-static-gray-white)` |
| `critical` | `status-critical-square-solid` | `var(--color-icon-alerting-critical)` | `var(--color-static-gray-white)` |
| `major-warning` | `status-error-diamond-solid` | `var(--color-icon-alerting-major)` | `var(--color-static-gray-white)` |
| `minor-warning` | `status-warn-tri-solid` | `var(--color-icon-alerting-minor)` | `var(--color-static-gray-white)` |
| `success` | `status-ok-circ-solid` | `var(--color-icon-alerting-success)` | `var(--color-static-gray-white)` |

## States (Light Theme)
| State | Background | Border | Text/Icon |
|---|---|---|---|
| Rest | `var(--color-static-gray-900)` | `1px solid var(--color-border-white)` | Message/link `var(--color-static-gray-white)` / `var(--color-text-white)`; close `var(--color-icon-white)`; icon per table above |
| Hover close | No root color change required | No change | Close affordance can use subtle focus-visible only (no color drift from tokens) |
| Focus-visible action | No root color change | No change | Focus ring: `var(--color-border-brand-base)` (see root-spec) |

## States (Dark Theme)
Structural parity with Light: same **semantic token names** on the shell; values resolve via `components/ids-ai-theme.css` under `[data-theme="dark"]`. Layout/spacing/radius tokens are unchanged unless Figma Dark explicitly overrides (Toast does not today).

| State | Background | Border | Text/Icon |
|---|---|---|---|
| Rest | `var(--color-static-gray-900)` | `1px solid var(--color-border-white)` | Message/link `var(--color-static-gray-white)` / `var(--color-text-white)`; close `var(--color-icon-white)`; status icon per mapping table |
| Hover close | No root color change | No change | Close: focus-visible only per root-spec |
| Focus-visible action | No root color change | No change | Focus ring: `var(--color-border-brand-base)` (see root-spec) |

**Note:** Re-validate these cells in Figma **Color Mode: Dark** when IDS-AI dark tokens change; hex resolution differs from Light while token **names** stay the same for this component.

## Interactions
- Toast appears with entrance motion and exits with dismissal motion.
- `close` click removes toast item and emits dismissal event.
- Optional link action emits link event or navigates depending on link contract.
- Auto-dismiss timer runs per toast item when `duration > 0`.
- Hover/focus pauses auto-dismiss timer for that item (recommended behavior contract).
- `Escape` dismisses focused toast item.

## Composition & API (runtime)

### `ToastItem` (single notification)
- `type`: `info | critical | major-warning | minor-warning | success` (default `info`).
- `message`: string (required).
- `duration`: number, default `8000` (host-configurable timeout input).
- `closable`: boolean, default `true`.
- `link`: optional structured link object (see link contract below).
- `onClose`: emitted with item id/reason.
- `onTimeout`: emitted when timer dismisses.

### `ToastViewport` (stack + queue owner)
- `position`: `top-left | top-center | top-right | bottom-left | bottom-center | bottom-right` (default `top-right`).
- `maxVisible`: number (recommended default `3`).
- `queueStrategy`: FIFO.
- `items`: controlled list OR internal queue adapter.

Queue/stack behavior contract:
1. New item is appended to queue.
2. If visible slots are full, item waits in queue.
3. When an item closes/expires, next queued item enters visible stack.
4. In top positions, newest visible toast appears at bottom; older items move upward.
5. In bottom positions, newest visible toast appears at top; older items move downward.

## Link Contract (framework-agnostic)
`link` object shape:
- `label: string` (required if link exists)
- `href?: string`
- `routerLink?: string | string[]`
- `target?: "_self" | "_blank" | "_parent" | "_top"`
- `onClick?: (event) => void`

Resolution rules:
1. Angular: if `routerLink` is present, render router navigation binding.
2. Else if `href` is present, render anchor navigation.
3. Else render button-style link and emit `onClick`.
4. If both `routerLink` and `href` are provided, prefer `routerLink`.

## Codegen Contract (Framework-Agnostic Blueprint)

### Deterministic anatomy
- `ToastViewport` -> repeated `ToastItem` -> `Content` + `ActionContainer`.
- `Content` always renders icon + message.
- `ActionContainer` renders optional `LinkAction`, then optional `CloseAction`.

### Variant matrix
- Supported types: `info`, `critical`, `major-warning`, `minor-warning`, `success`.
- `link` optional for each type.
- `duration` and `position` supported for all combinations.

### Per-slot styling rules
- Root/background/border/text/close must use semantic tokens defined in this spec.
- Status icon must use icon component with `shapeName` from mapping table.
- Status icon slot and rendered glyph must both remain `16x16`; message alignment must remain vertically centered with icon.
- Icon assets must resolve from `assets/icons`; no inline SVG or `data:image/svg+xml`.

### Behavior rules
- Timer lifecycle: start on mount, pause on hover/focus, resume on unhover/blur, clear on unmount.
- Dismissal reasons: `close-click | timeout | programmatic`.
- Queue lifecycle: deterministic FIFO as defined above.

### Accessibility rules
- Viewport: `aria-live="polite"`, `aria-atomic="false"`.
- Item role: `status` by default; allow severity-driven escalation to `alert` when product rules require.
- Close action: accessible label ("Dismiss notification").
- Keyboard: tab to link/close, `Escape` dismiss focused item.

### Fallback/error rules
- Unknown `type` -> fallback to `info`.
- Missing icon asset -> render no icon and log non-blocking warning.
- Invalid `position` -> fallback to `top-right`.
- Invalid `duration` (<0 or NaN) -> fallback to `8000`.

### Validation checklist (pass/fail)
- [ ] All required sections in this spec are complete and deterministic.
- [ ] Variant to token/icon mapping matches table exactly.
- [ ] Queue behavior is implemented at viewport level, not single item level.
- [ ] `position` API supports all six values with default `top-right`.
- [ ] Link routing contract supports `routerLink`, `href`, and event mode.
- [ ] Close icon is `shape-x` at `12x12`.
- [ ] No hardcoded style values in generated code where token exists.

## Source Mapping
- Figma component: `10823:74993` (IDS-AI Toast symbol).
- Figma scenarios: `43339:4503` (`content` frame — instance widths `516` / `617` × height `48`).
- Figma element reference: `44506:257602` (`.Toast icons` — five `16×16` severity symbols).
- **Live verification (2026-05-10):** Figma MCP **`get_design_context`**, **`get_variable_defs`**, **`get_metadata`** on `fileKey=rr8F0NGA6RNY9Z0cNeh3jh` for nodes `10823:74993`, `43339:4503`, `44506:257602`. Confirmed variable bindings on the main instance include: `Color/Static/Gray/900`, `Color/Border/White`, `Border Width/border-default`, `Corner Radius/radius-8`, `Spacing/space-32`, `Spacing/space-8`, `Spacing/space-24`, `Padding/padding-24`, `Padding/padding-14`, `Padding/padding-2`, `Color/Static/Gray/White`, `Color/Text/White`, `Color/Icon/White`, `Font Size/body-2`, `Font Line Height/line-height-20`, `Typography/Font Style/primary`, `Sizing/size-12`, `Sizing/size-16`.
