## Metadata

Content for Metadata.

Use `var(--color-text-primary)`.

## Layout & Measurements

Content for Layout & Measurements.

Use `var(--color-text-primary)`.

### Slot geometry (Figma-verified)

| Slot | Property | Value | Node |
| --- | --- | --- | --- |
| Container | border-radius | `var(--radius-md)` | stub-main |

## Tokens

Content for Tokens.

Use `var(--color-text-primary)`.

## States (Light Theme)

Content for States (Light Theme).

Use `var(--color-text-primary)`.

## States (Dark Theme)

Content for States (Dark Theme).

Use `var(--color-text-primary)`.

## Interactions

Content for Interactions.

Use `var(--color-text-primary)`.

## Composition & API (runtime)

Content for Composition & API (runtime).

Use `var(--color-text-primary)`.

## Codegen Contract (Framework-Agnostic Blueprint)

Content for Codegen Contract (Framework-Agnostic Blueprint).

Use `var(--color-text-primary)`.

## Source Mapping

Content for Source Mapping.

Use `var(--color-text-primary)`.

File key + node ids from figma_evidence (stub).
