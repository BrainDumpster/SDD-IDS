# Accordion Design Spec

## Metadata
- Component: Accordion
- Design System: Synapse
- Category: Form Elements
- Figma URL: https://www.figma.com/design/Td1bnsvRj1PCGs9RVJkIvJ/Synapse-Hi-Fi-components?node-id=16551-26036&m=dev
- File key: `Td1bnsvRj1PCGs9RVJkIvJ`
- Primary node: `16551:26036`
- Includes scenarios: all collapsed, one expanded, multi expanded, accordion with form

## Anatomy
Main component:
- `AccordionRoot`

Child components (deterministic order):
1. `AccordionItem`
2. `AccordionHeader`
3. `AccordionTrigger`
4. `AccordionChevron` (left or right slot)
5. `AccordionBody` (framework alias: `AccordionPanel`)
6. `AccordionContent`

Optional child sub-slots:
- `AccordionFormSlot` (for "accordion with form" usage)
- `AccordionMetaSlot` (optional helper text/metadata)

## Layout & Measurements
- Root width is container-driven (`width: 100%`).
- Item separators: `var(--border-width-border-1)` using neutral divider token.
- Trigger row uses horizontal padding token pair (`12/16` pattern in current implementation).
- Trigger/content gap and block rhythm follow `var(--spacing-space-8)` and body text line-height contracts.
- Chevron can be placed on:
  - `left` (leading; default)
  - `right` (trailing)
- Expanded trigger renders a 4px left highlighter using `var(--color-border-brand-base)`.
- Focus indicator must be visible on trigger row and not clipped by item container.

## Tokens
- Surface:
  - `var(--color-background-component)`
  - `var(--color-background-surface-1)`
- Borders:
  - `var(--border-width-border-1)`
  - `var(--border-width-border-2)`
  - `var(--color-border-accessible)`
  - `var(--color-border-brand-base)`
- Text/Icon:
  - `var(--color-text-neutral-strong)`
  - `var(--color-text-neutral)`
  - `var(--color-icon-neutral)`
- Icon asset:
  - canonical chevron icon name: `chev-down-thick`
- Radius/spacing/type:
  - `var(--corner-radius-radius-4)`
  - `var(--padding-padding-12)`
  - `var(--padding-padding-16)`
  - `var(--font-size-body-2)`
  - `var(--font-line-height-line-height-20)`

## States (Light Theme)
| Slot | State | Background | Border | Text/Icon |
|---|---|---|---|---|
| trigger | default (collapsed) | `var(--color-background-component)` | item divider `var(--color-border-accessible)` | title `var(--color-text-neutral-strong)`, chevron `var(--color-icon-neutral)` |
| trigger | hover (collapsed) | `var(--color-background-brand-lighter)` | unchanged divider | title `var(--color-text-neutral-strong)`, chevron `var(--color-border-strong)` |
| trigger | hover (expanded) | `var(--color-background-brand-light)` | unchanged divider, left bar `var(--color-border-brand-base)` | title `var(--color-text-neutral-strong)`, chevron `var(--color-border-strong)` |
| trigger | focus-visible | same as current open/closed state | outer focus ring `var(--border-width-border-2)` `var(--color-border-brand-base)` | same as default |
| trigger | disabled | same as base state | unchanged | reduced emphasis (`opacity` contract) + non-interactive cursor |
| panel/content | expanded | `var(--color-background-component)` | item divider `var(--color-border-accessible)`, left bar `var(--color-border-brand-base)` | body `var(--color-text-neutral)` |

## States (Dark Theme)
| Slot | State | Background | Border | Text/Icon |
|---|---|---|---|---|
| trigger | default (collapsed) | `var(--color-background-component)` | item divider `var(--color-border-accessible)` | title `var(--color-text-neutral-strong)`, chevron `var(--color-icon-neutral)` |
| trigger | hover (collapsed) | `var(--color-background-brand-lighter)` | unchanged divider | title `var(--color-text-neutral-strong)`, chevron `var(--color-border-strong)` |
| trigger | hover (expanded) | `var(--color-background-brand-light)` | unchanged divider, left bar `var(--color-border-brand-base)` | title `var(--color-text-neutral-strong)`, chevron `var(--color-border-strong)` |
| trigger | focus-visible | same as current open/closed state | outer focus ring `var(--border-width-border-2)` `var(--color-border-brand-base)` | same as default |
| trigger | disabled | same as base state | unchanged | reduced emphasis (`opacity` contract) + non-interactive cursor |
| panel/content | expanded | `var(--color-background-component)` | item divider `var(--color-border-accessible)`, left bar `var(--color-border-brand-base)` | body `var(--color-text-neutral)` |

## Interactions
- Trigger activation toggles panel expansion.
- Single-expand mode (`multiple=false`) keeps max one open panel.
- Multi-expand mode (`multiple=true`) allows more than one open panel.
- Disabled item is non-interactive.
- Arrow key roving focus is supported between triggers.
- Home/End move focus to first/last trigger.
- Panel content participates in layout only when expanded.

## Composition & API (runtime)
Main inputs:
- `items: AccordionItemInput[]` (required)
- `multiple?: boolean` (default `false`)
- `defaultValue?: string[]`
- `chevronPosition?: "left" | "right"` (default `"left"`)
- `variant?: "default" | "form"` (default `"default"`)

Per-item inputs:
- `value: string` (required stable ID)
- `title: string` (required)
- `content: RenderableNode` (required; framework-native renderable/content type)
- `disabled?: boolean`
- `meta?: RenderableNode`
- `formSlot?: RenderableNode`

Outputs:
- `onValueChange?(openValues: string[])`

## Codegen Contract (Framework-Agnostic Blueprint)
Deterministic structure:
- `AccordionRoot`
  - repeated `AccordionItem`
    - `AccordionHeader`
      - `AccordionTrigger`
        - optional leading `AccordionChevron` (left variant)
        - `title`
        - optional trailing `AccordionChevron` (right variant)
    - `AccordionBody` (aka `AccordionPanel` for libraries that use panel naming)
      - `AccordionContent`
      - optional `AccordionMetaSlot`
      - optional `AccordionFormSlot`

Variant matrix:
- `variant`: `default | form`
- `chevronPosition`: `left | right`
- expand behavior: `single | multiple` (resolved from user input)
- item state: `collapsed | expanded | disabled`

Behavior contract:
- If `multiple=false`, opening one item closes the previously open item.
- If `multiple=true`, toggles item independently.
- `disabled` prevents toggle and focus activation by keyboard/pointer.

Accessibility contract:
- Trigger carries `aria-expanded` and `aria-controls`.
- Panel carries `role="region"` and `aria-labelledby`.
- Keyboard support: `Enter`, `Space`, `ArrowUp`, `ArrowDown`, `Home`, `End`.

Fallback/error rules:
- Unknown `chevronPosition` -> fallback to `"left"`.
- Unknown `variant` -> fallback to `"default"`.
- Missing `value` or duplicate `value` entries are validation errors.

Validation checklist:
- [ ] Generated hierarchy is `Accordion -> AccordionItem -> AccordionHeader -> AccordionBody`.
- [ ] All state rows map to semantic tokens (light/dark parallel).
- [ ] Single and multi expand behavior both implemented.
- [ ] Chevron renders correctly in both left and right positions.
- [ ] Focus-visible ring is visible and not clipped.
- [ ] Disabled rows are non-interactive.
- [ ] `form` variant supports form slot without breaking structure.

## Source Mapping
| Source | Location |
|---|---|
| Root spec | `components/synapse/root-spec.mdx` |
| Theme CSS | `components/synapse-theme.css` |
| Component map | `data/synapse-component-figma-map.json` |
| Storybook implementation | `storybook/src/components/Accordion.tsx`, `storybook/src/components/Accordion.module.css` |
