/* @generated by strict-spec-storybook-gate */
/* component: tooltip */
/* spec_hash: d87ad38f434d3b1eb37145a15f4ef9e6e5dca46c360ebc00407a27119c25422e */
import type { Meta, StoryObj } from "@storybook/react";
import { IdsTooltip as IDSAITooltip } from "../../../../storybook/src/components/IdsTooltip";

const meta: Meta<typeof IDSAITooltip> = {
  title: "Spec Generated/IDS-AI/Tooltip",
  component: IDSAITooltip,
  parameters: { layout: "centered" },
  args: {
    content: "Tooltip body content",
    title: "Tooltip title",
    side: "top",
    align: "center",
    showArrow: true,
    closable: false,
  },
  argTypes: {
    title: { control: "text" },
    content: { control: "text" },
    side: { control: "select", options: ["top", "bottom", "left", "right"] },
    align: { control: "select", options: ["start", "center", "end"] },
    showArrow: { control: "boolean" },
    closable: { control: "boolean" },
  },
};

export default meta;
type Story = StoryObj<typeof IDSAITooltip>;

function TooltipStoryStyles() {
  return (
    <style>{
      `
      .sbTooltipCanvas {
        display: flex;
        min-height: 180px;
        align-items: center;
        justify-content: center;
        padding: 32px;
      }
      .sbTooltipRow {
        display: flex;
        gap: 28px;
        flex-wrap: wrap;
        align-items: center;
        justify-content: center;
        min-height: 180px;
      }
      .sbTooltipGrid {
        display: grid;
        grid-template-columns: 1fr;
        gap: 14px;
        min-width: 860px;
        max-width: 1100px;
        padding: 8px;
      }
      .sbTooltipSideRow {
        display: grid;
        grid-template-columns: 88px repeat(3, minmax(220px, 1fr));
        gap: 14px;
        align-items: stretch;
      }
      .sbTooltipSideTitle {
        display: flex;
        align-items: center;
        justify-content: flex-start;
        font-size: 12px;
        text-transform: uppercase;
        letter-spacing: 0.04em;
        opacity: 0.9;
        padding-left: 4px;
      }
      .sbTooltipCell {
        display: grid;
        gap: 10px;
        justify-items: center;
        align-content: center;
        min-height: 120px;
        padding: 8px;
        border: 1px dashed var(--color-border-neutral-light, #c5c5c5);
        border-radius: 6px;
        background: var(--color-background-component, #ffffff);
      }
      .sbTooltipLabel {
        font-size: 12px;
        opacity: 0.9;
        text-transform: capitalize;
      }
      .sbTooltipTrigger {
        padding: 7px 14px;
        border: 1px solid var(--color-border-accessible);
        background: var(--color-background-component);
        color: var(--color-text-neutral-strong);
        border-radius: 2px;
        font-size: 14px;
      }
      `
    }</style>
  );
}

export const Playground: Story = {
  render: (args) => (
    <div className="sbTooltipCanvas">
      <TooltipStoryStyles />
      <IDSAITooltip {...args}>
        <button type="button" className="sbTooltipTrigger">Hover / Focus me</button>
      </IDSAITooltip>
    </div>
  ),
};

export const StandardAndClosable: Story = {
  render: () => (
    <div className="sbTooltipRow">
      <TooltipStoryStyles />
      <IDSAITooltip
        title="Standard"
        content="Standard tooltip closes on leave/blur."
        side="top"
        align="center"
      >
        <button type="button" className="sbTooltipTrigger">Standard</button>
      </IDSAITooltip>
      <IDSAITooltip
        title="Closable"
        content="Closable tooltip stays open until close icon is clicked."
        closable
        side="top"
        align="center"
      >
        <button type="button" className="sbTooltipTrigger">Closable</button>
      </IDSAITooltip>
    </div>
  ),
};

export const PlacementMatrix: Story = {
  render: () => {
    const sides = ["top", "bottom", "left", "right"] as const;
    const aligns = ["start", "center", "end"] as const;
    return (
      <div className="sbTooltipGrid">
        <TooltipStoryStyles />
        {sides.map((side) => (
          <div key={side} className="sbTooltipSideRow">
            <div className="sbTooltipSideTitle">{side}</div>
            {aligns.map((align) => (
              <div key={`${side}-${align}`} className="sbTooltipCell">
                <div className="sbTooltipLabel">{align}</div>
                <IDSAITooltip
                  side={side}
                  align={align}
                  title="Tooltip"
                  content={"Placement: " + side + "-" + align}
                >
                  <button type="button" className="sbTooltipTrigger">Trigger</button>
                </IDSAITooltip>
              </div>
            ))}
          </div>
        ))}
      </div>
    );
  },
};

export const BodyOnly: Story = {
  render: () => (
    <div className="sbTooltipCanvas">
      <TooltipStoryStyles />
      <IDSAITooltip
        content="Body-only tooltip content with no title."
        side="right"
        align="center"
      >
        <button type="button" className="sbTooltipTrigger">Body only</button>
      </IDSAITooltip>
    </div>
  ),
};

export const LayoutTokens: Story = {
  render: () => (
    <TooltipStoryStyles />
  ),
};

const specTokens = ["--color-background-surface-2", "--color-border-accessible", "--color-text-neutral-strong", "--color-text-neutral", "--border-width-border-default", "--corner-radius-radius-8"] as const;
const specTokenRefs = {"--color-background-surface-2": ["Tokens: - Panel background: `var(--color-background-surface-2)`.", "States (Light Theme): | Standard (no title) | `var(--color-background-surface-2)` | `var(--color-border-accessible)` | body `var(--color-te...", "States (Light Theme): | With header | `var(--color-background-surface-2)` | `var(--color-border-accessible)` | header `var(--color-text-neu...", "States (Light Theme): | Closable | `var(--color-background-surface-2)` | `var(--color-border-accessible)` | header/body as above; close ico...", "States (Dark Theme): | Standard (no title) | `var(--color-background-surface-2)` (dark-resolved) | `var(--color-border-accessible)` (dark-...", "States (Dark Theme): | With header | `var(--color-background-surface-2)` (dark-resolved) | `var(--color-border-accessible)` (dark-resolved...", "States (Dark Theme): | Closable | `var(--color-background-surface-2)` (dark-resolved) | `var(--color-border-accessible)` (dark-resolved) |..."], "--color-border-accessible": ["Tokens: - Panel border + arrow stroke: `var(--color-border-accessible)`.", "States (Light Theme): | Standard (no title) | `var(--color-background-surface-2)` | `var(--color-border-accessible)` | body `var(--color-te...", "States (Light Theme): | With header | `var(--color-background-surface-2)` | `var(--color-border-accessible)` | header `var(--color-text-neu...", "States (Light Theme): | Closable | `var(--color-background-surface-2)` | `var(--color-border-accessible)` | header/body as above; close ico...", "States (Dark Theme): | Standard (no title) | `var(--color-background-surface-2)` (dark-resolved) | `var(--color-border-accessible)` (dark-...", "States (Dark Theme): | With header | `var(--color-background-surface-2)` (dark-resolved) | `var(--color-border-accessible)` (dark-resolved...", "States (Dark Theme): | Closable | `var(--color-background-surface-2)` (dark-resolved) | `var(--color-border-accessible)` (dark-resolved) |..."], "--color-text-neutral-strong": ["Tokens: - Header text: `var(--color-text-neutral-strong)`.", "States (Light Theme): | With header | `var(--color-background-surface-2)` | `var(--color-border-accessible)` | header `var(--color-text-neu...", "States (Dark Theme): | With header | `var(--color-background-surface-2)` (dark-resolved) | `var(--color-border-accessible)` (dark-resolved..."], "--color-text-neutral": ["Tokens: - Body text: `var(--color-text-neutral)`.", "States (Light Theme): | Standard (no title) | `var(--color-background-surface-2)` | `var(--color-border-accessible)` | body `var(--color-te...", "States (Light Theme): | With header | `var(--color-background-surface-2)` | `var(--color-border-accessible)` | header `var(--color-text-neu...", "States (Dark Theme): | Standard (no title) | `var(--color-background-surface-2)` (dark-resolved) | `var(--color-border-accessible)` (dark-...", "States (Dark Theme): | With header | `var(--color-background-surface-2)` (dark-resolved) | `var(--color-border-accessible)` (dark-resolved..."], "--border-width-border-default": ["Layout & Measurements: - Panel border: `1px solid` (`var(--border-width-border-default)`)."], "--corner-radius-radius-8": ["Layout & Measurements: - Border radius tokenized: `var(--corner-radius-radius-8)` for IDS-AI tooltip surface."]} as Record<string, string[]>;

export const TokenInspector: Story = {
  render: () => (
    <div className="sbTokenInspector">
      <style>{
        `
        .sbTokenInspector {
          display: grid;
          gap: 8px;
          max-width: 880px;
        }
        .sbTokenHeader {
          font-size: 12px;
          opacity: 0.8;
        }
        .sbTokenRow {
          display: grid;
          grid-template-columns: minmax(260px, 1fr) 72px 120px minmax(260px, 1fr);
          align-items: start;
          gap: 12px;
          padding: 6px 8px;
          border: 1px solid var(--color-border-neutral-light, #c5c5c5);
          border-radius: 4px;
          background: var(--color-background-component, #ffffff);
        }
        .sbTokenCode {
          font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, "Liberation Mono", "Courier New", monospace;
          font-size: 12px;
          white-space: nowrap;
          overflow: hidden;
          text-overflow: ellipsis;
        }
        .sbTokenSwatch {
          width: 64px;
          height: 20px;
          border: 1px solid var(--color-border-accessible, #757575);
          border-radius: 2px;
          background: transparent;
        }
        .sbTokenSample {
          font-size: 12px;
          white-space: nowrap;
        }
        .sbTokenRefs {
          display: grid;
          gap: 2px;
          font-size: 11px;
          line-height: 16px;
          opacity: 0.9;
        }
        .sbTokenRef {
          white-space: nowrap;
          overflow: hidden;
          text-overflow: ellipsis;
        }
        `
      }</style>
      <div className="sbTokenHeader">Spec token inspector (name + live preview + where token is referenced in spec)</div>
      {specTokens.map((token) => (
        <div key={token} className="sbTokenRow">
          <span className="sbTokenCode">{`var(${token})`}</span>
          <span className="sbTokenSwatch" style={{ background: `var(${token})` }} />
          <span className="sbTokenSample" style={{ color: `var(${token})` }}>Sample</span>
          <div className="sbTokenRefs">
            {(specTokenRefs[token] || []).slice(0, 3).map((ref, idx) => (
              <div key={`${token}-${idx}`} className="sbTokenRef" title={ref}>{ref}</div>
            ))}
            {(specTokenRefs[token] || []).length === 0 ? <div className="sbTokenRef">No direct spec reference found</div> : null}
            {(specTokenRefs[token] || []).length > 3 ? (
              <div className="sbTokenRef">+{(specTokenRefs[token] || []).length - 3} more</div>
            ) : null}
          </div>
        </div>
      ))}
    </div>
  ),
};

