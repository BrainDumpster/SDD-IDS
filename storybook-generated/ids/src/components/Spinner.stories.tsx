/* @generated by strict-spec-storybook-gate */
/* component: spinner */
/* spec_hash: d24e947aa398f54713287f86f6b850802d0b4eb402d8866cd5d0b33443434c83 */
import type { Meta, StoryObj } from "@storybook/react";
import { IdsSpinner as IdsSpinner } from "../../../../storybook/src/components/IdsSpinner";

const meta: Meta<typeof IdsSpinner> = {
  title: "Spec Generated/IDS/Spinner",
  component: IdsSpinner,
  argTypes: {
    size: { control: "select", options: ["sm", "md", "lg"] },
    labelVisibility: { control: "select", options: ["sr-only", "inline", "below"] },
  },
};

export default meta;
type Story = StoryObj<typeof IdsSpinner>;

export const Small: Story = {
  args: { size: "sm", labelVisibility: "inline", label: "Loading..." },
};

export const Medium: Story = {
  args: { size: "md", labelVisibility: "below", label: "Loading..." },
};

export const Large: Story = {
  args: { size: "lg", labelVisibility: "sr-only", label: "Loading..." },
};

export const FigmaUsageFrame: Story = {
  render: () => (
    <div style={{ display: "flex", gap: 24, alignItems: "center", padding: 16 }}>
      <IdsSpinner size="sm" labelVisibility="inline" label="Loading..." />
      <IdsSpinner size="md" labelVisibility="below" label="Loading..." />
      <IdsSpinner size="lg" labelVisibility="sr-only" label="Loading..." />
    </div>
  ),
};

const specTokens = ["--color-border-brand-base", "--color-background-surface-2", "--color-text-neutral-strong", "--color-static-gray-white", "--color-static-brand-500", "--color-background-overlay"] as const;
const specTokenRefs = {"--color-border-brand-base": ["Tokens: - Spinner arc (brand): `var(--color-border-brand-base)` \u2192 resolves to `#0076ce` in light theme.", "Tokens: - Static brand 500: `var(--color-static-brand-500)` = `#0076ce` (matches `var(--color-border-brand-base)`).", "Tokens: - Stroke color: `var(--color-border-brand-base)`", "States (Light Theme): | `inline` | Spinner arc | transparent | `var(--color-border-brand-base)` | n/a | Continuous rotation |", "States (Light Theme): | `overlay` | Spinner arc | transparent | `var(--color-border-brand-base)` | n/a | Continuous rotation |", "States (Dark Theme): | `inline` | Spinner arc | transparent | `var(--color-border-brand-base)` | n/a | Brand arc remains vivid on dark bac...", "States (Dark Theme): | `overlay` | Spinner arc | transparent | `var(--color-border-brand-base)` | n/a | Same as light theme. |", "Codegen Contract (Framework-Agnostic Blueprint): - Arc: `var(--color-border-brand-base)`.", "Source Mapping: - `var(--color-border-brand-base)` = `#0076ce`"], "--color-background-surface-2": ["Tokens: - Spinner outer circle background / track: `var(--color-background-surface-2)` \u2192 resolves to `#ffffff` in light theme.", "Tokens: - Fill: `var(--color-background-surface-2)`", "States (Light Theme): | `inline` | Inner disc | `var(--color-background-surface-2)` | none | n/a | none |", "States (Dark Theme): | `inline` | Inner disc | `var(--color-background-surface-2)` | none | n/a | Token resolves for dark theme surface co...", "Codegen Contract (Framework-Agnostic Blueprint): - Inner disc/track: `var(--color-background-surface-2)`.", "Source Mapping: - `var(--color-background-surface-2)` = `#ffffff`"], "--color-text-neutral-strong": ["Tokens: - Loading text: `var(--color-text-neutral-strong)` \u2192 resolves to `#252525` in light theme.", "Tokens: - Color: `var(--color-text-neutral-strong)`", "States (Light Theme): | `inline` | Loading text | transparent | none | `var(--color-text-neutral-strong)` | none |", "States (Light Theme): | `overlay` | Loading text | transparent | none | `var(--color-text-neutral-strong)` | none |", "States (Dark Theme): | `inline` | Loading text | transparent | none | `var(--color-text-neutral-strong)` | Ensures AA contrast on dark sur...", "States (Dark Theme): | `overlay` | Loading text | transparent | none | `var(--color-text-neutral-strong)` | Same semantic token; resolves ...", "Codegen Contract (Framework-Agnostic Blueprint): - Text: `var(--color-text-neutral-strong)`.", "Source Mapping: - `var(--color-text-neutral-strong)` = `#252525`"], "--color-static-gray-white": ["Tokens: - Static white: `var(--color-static-gray-white)` = `#ffffff` (used in Figma art; runtime uses semantic surface tokens)."], "--color-static-brand-500": ["Tokens: - Static brand 500: `var(--color-static-brand-500)` = `#0076ce` (matches `var(--color-border-brand-base)`)."], "--color-background-overlay": ["States (Light Theme): | `overlay` | Backdrop | `var(--color-background-overlay)` | none | n/a | none |", "States (Dark Theme): | `overlay` | Backdrop | semantic overlay surface token (e.g. `var(--color-background-overlay)`) | none | n/a | Dims ...", "Codegen Contract (Framework-Agnostic Blueprint): - Overlay backdrop: `var(--color-background-overlay)` when `mode=\"overlay\"`.", "Token Gaps / Notes: - Overlay color token (`var(--color-background-overlay)`) should be referenced from the global theme definition.", "Source Mapping: - `var(--color-background-overlay)` = design-system overlay token (resolved via active theme)"]} as Record<string, string[]>;

export const TokenInspector: Story = {
  render: () => (
    <div className="sbTokenInspector">
      <style>{
        `
        .sbTokenInspector {
          display: grid;
          gap: 8px;
          max-width: 880px;
        }
        .sbTokenHeader {
          font-size: 12px;
          opacity: 0.8;
        }
        .sbTokenRow {
          display: grid;
          grid-template-columns: minmax(260px, 1fr) 72px 120px minmax(260px, 1fr);
          align-items: start;
          gap: 12px;
          padding: 6px 8px;
          border: 1px solid var(--color-border-neutral-light, #c5c5c5);
          border-radius: 4px;
          background: var(--color-background-component, #ffffff);
        }
        .sbTokenCode {
          font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, "Liberation Mono", "Courier New", monospace;
          font-size: 12px;
          white-space: nowrap;
          overflow: hidden;
          text-overflow: ellipsis;
        }
        .sbTokenSwatch {
          width: 64px;
          height: 20px;
          border: 1px solid var(--color-border-accessible, #757575);
          border-radius: 2px;
          background: transparent;
        }
        .sbTokenSample {
          font-size: 12px;
          white-space: nowrap;
        }
        .sbTokenRefs {
          display: grid;
          gap: 2px;
          font-size: 11px;
          line-height: 16px;
          opacity: 0.9;
        }
        .sbTokenRef {
          white-space: nowrap;
          overflow: hidden;
          text-overflow: ellipsis;
        }
        `
      }</style>
      <div className="sbTokenHeader">Spec token inspector (name + live preview + where token is referenced in spec)</div>
      {specTokens.map((token) => (
        <div key={token} className="sbTokenRow">
          <span className="sbTokenCode">{`var(${token})`}</span>
          <span className="sbTokenSwatch" style={{ background: `var(${token})` }} />
          <span className="sbTokenSample" style={{ color: `var(${token})` }}>Sample</span>
          <div className="sbTokenRefs">
            {(specTokenRefs[token] || []).slice(0, 3).map((ref, idx) => (
              <div key={`${token}-${idx}`} className="sbTokenRef" title={ref}>{ref}</div>
            ))}
            {(specTokenRefs[token] || []).length === 0 ? <div className="sbTokenRef">No direct spec reference found</div> : null}
            {(specTokenRefs[token] || []).length > 3 ? (
              <div className="sbTokenRef">+{(specTokenRefs[token] || []).length - 3} more</div>
            ) : null}
          </div>
        </div>
      ))}
    </div>
  ),
};

