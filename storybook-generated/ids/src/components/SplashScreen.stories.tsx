/* @generated by strict-spec-storybook-gate */
/* component: splash-screen */
import React, { type ComponentProps } from "react";
import type { Meta, StoryObj } from "@storybook/react";
import "../../../../components/ids-theme.css";
import { IdsSplashScreen } from "../../../../storybook/src/components/IdsSplashScreen";
import { Icon } from "../../../../storybook/src/components/Icon";

const DESIGN_SPEC_PATH = "components/ids/splash-screen/design-spec.md";

const DEFAULT_LOGO = (
  <Icon
    shapeName="logo-delltech-stacked-white"
    variant="img"
    style={{ width: 120, height: 120 }}
  />
);

const meta: Meta<typeof IdsSplashScreen> = {
  title: "Spec Generated/IDS/Splash Screen",
  component: IdsSplashScreen,
  parameters: {
    layout: "padded",
    docs: {
      description: {
        component: `IDS Splash Screen per \`${DESIGN_SPEC_PATH}\`. Theme: \`components/ids-theme.css\`.`,
      },
    },
  },
  argTypes: {
    appName: { control: "text" },
    statusText: { control: "text" },
    version: { control: "text" },
    progress: { control: { type: "range", min: 0, max: 100 } },
    animated: { control: "boolean" },
    variant: {
      control: "select",
      options: [
        "default",
        "minimal",
        "with-progress",
        "with-animation",
        "with-skip",
        "with-version",
        "interactive",
      ],
    },
    skipLabel: { control: "text" },
    interactiveLabel: { control: "text" },
  },
  args: {
    appName: "Dell Application",
    statusText: "Loading...",
    animated: false,
    variant: "default",
    logo: DEFAULT_LOGO,
  },
};

export default meta;
type Story = StoryObj<typeof IdsSplashScreen>;

function StoryFrame(props: ComponentProps<typeof IdsSplashScreen>) {
  return (
    <div
      style={{
        position: "relative",
        width: 360,
        height: 360,
        overflow: "hidden",
        border: "1px solid var(--color-border-neutral-light, #c5c5c5)",
        borderRadius: 4,
      }}
    >
      <IdsSplashScreen {...props} />
    </div>
  );
}

/** Canonical spec default — full-screen brand splash with logo, title and spinner. */
export const SpecAccurateDesign: Story = {
  name: "Spec Accurate Design",
  args: {
    appName: "Dell Application",
    statusText: "Loading your experience...",
    variant: "default",
    logo: DEFAULT_LOGO,
  },
  parameters: { layout: "fullscreen" },
};

/** Interactive controls playground. */
export const Playground: Story = {
  render: (args: ComponentProps<typeof IdsSplashScreen>) => (
    <StoryFrame {...args} />
  ),
};

/** Variant matrix from the design spec. */
export const Variants: Story = {
  render: () => (
    <div
      style={{
        display: "grid",
        gridTemplateColumns: "repeat(auto-fit, minmax(360px, 1fr))",
        gap: 16,
        padding: 16,
      }}
    >
      <StoryFrame
        appName="Default"
        variant="default"
        statusText="Loading..."
        logo={DEFAULT_LOGO}
      />
      <StoryFrame appName="Minimal" variant="minimal" logo={DEFAULT_LOGO} />
      <StoryFrame
        appName="With Progress"
        variant="with-progress"
        progress={45}
        statusText="Loading..."
        logo={DEFAULT_LOGO}
      />
      <StoryFrame
        appName="With Animation"
        variant="with-animation"
        statusText="Launching..."
        animated
        logo={DEFAULT_LOGO}
      />
      <StoryFrame
        appName="With Version"
        variant="with-version"
        version="Version 2.1.0"
        statusText="Loading..."
        logo={DEFAULT_LOGO}
      />
      <StoryFrame
        appName="With Skip"
        variant="with-skip"
        onSkip={() => {}}
        statusText="Loading..."
        logo={DEFAULT_LOGO}
      />
      <StoryFrame
        appName="Interactive"
        variant="interactive"
        onInteractive={() => {}}
        interactiveLabel="Retry"
        statusText="Unable to load"
        logo={DEFAULT_LOGO}
      />
    </div>
  ),
};

/** Dark theme variant using the Storybook theme toolbar. */
export const DarkTheme: Story = {
  name: "Dark Theme",
  args: {
    appName: "Dell Application",
    statusText: "Loading...",
    variant: "default",
    logo: DEFAULT_LOGO,
  },
  globals: { theme: "dark" },
  parameters: { layout: "fullscreen" },
};

const specTokens = [
  "--color-text-white",
  "--color-background-brand-base",
  "--color-background-controls-brand-base",
  "--color-background-white",
  "--color-border-white",
  "--color-border-brand-base",
  "--color-icon-white",
  "--color-static-gray-white",
  "--color-static-brand-500",
  "--font-size-body-2",
  "--border-width-border-2",
] as const;

const specTokenRefs: Record<string, string[]> = {
  "--color-text-white": [
    "Tokens: - White: `var(--color-text-white)` = #ffffff",
    "Tokens: - White text: `var(--color-text-white)` = #ffffff",
    "States (Light Theme): Splash Container text",
  ],
  "--color-background-brand-base": [
    "Tokens: - Brand background: `var(--color-background-brand-base)` = #0076ce",
    "States (Light Theme): Splash Container background",
  ],
  "--color-background-controls-brand-base": [
    "Tokens: - Brand base: `var(--color-background-controls-brand-base)` = #0076ce",
    "UI Palettes: Dell Blue 500: #0076CE",
  ],
  "--color-background-white": [
    "Tokens: - White background: `var(--color-background-white)` = #ffffff",
    "States (Light Theme): Interactive Elements background",
  ],
  "--color-border-white": [
    "Tokens: - White border: `var(--color-border-white)` = #ffffff",
    "States (Light Theme): Splash Container border",
  ],
  "--color-border-brand-base": [
    "Tokens: - Brand border: `var(--color-border-brand-base)` = #0076ce",
    "States (Light Theme): Interactive Elements border",
  ],
  "--color-icon-white": [
    "Tokens: - White icon: `var(--color-icon-white)` = #ffffff",
  ],
  "--color-static-gray-white": [
    "Tokens: - Static white: `var(--color-static-gray-white)` = #ffffff",
  ],
  "--color-static-brand-500": [
    "Tokens: - Static brand 500: `var(--color-static-brand-500)` = #0076ce",
    "UI Palettes: Dell Blue 500: #0076CE",
  ],
  "--font-size-body-2": [
    "Typography: Body 2: Roboto Regular 14px/1.5 (status text, version info)",
  ],
  "--border-width-border-2": [
    "Layout & Measurements: Focus ring 2px offset from interactive elements",
  ],
};

export const TokenInspector: Story = {
  render: () => (
    <div className="sbTokenInspector">
      <style>{`
        .sbTokenInspector {
          display: grid;
          gap: 8px;
          max-width: 880px;
        }
        .sbTokenHeader {
          font-size: 12px;
          opacity: 0.8;
        }
        .sbTokenRow {
          display: grid;
          grid-template-columns: minmax(260px, 1fr) 72px 120px minmax(260px, 1fr);
          align-items: start;
          gap: 12px;
          padding: 6px 8px;
          border: 1px solid var(--color-border-neutral-light, #c5c5c5);
          border-radius: 4px;
          background: var(--color-background-component, #ffffff);
        }
        .sbTokenCode {
          font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, "Liberation Mono", "Courier New", monospace;
          font-size: 12px;
          white-space: nowrap;
          overflow: hidden;
          text-overflow: ellipsis;
        }
        .sbTokenSwatch {
          width: 64px;
          height: 20px;
          border: 1px solid var(--color-border-accessible, #757575);
          border-radius: 2px;
          background: transparent;
        }
        .sbTokenSample {
          font-size: 12px;
          white-space: nowrap;
        }
        .sbTokenRefs {
          display: grid;
          gap: 2px;
          font-size: 11px;
          line-height: 16px;
          opacity: 0.9;
        }
        .sbTokenRef {
          white-space: nowrap;
          overflow: hidden;
          text-overflow: ellipsis;
        }
      `}</style>
      <div className="sbTokenHeader">Spec token inspector (name + live preview + where token is referenced in spec)</div>
      {specTokens.map((token) => (
        <div key={token} className="sbTokenRow">
          <span className="sbTokenCode">{`var(${token})`}</span>
          <span className="sbTokenSwatch" style={{ background: `var(${token})` }} />
          <span className="sbTokenSample" style={{ color: `var(${token})` }}>Sample</span>
          <div className="sbTokenRefs">
            {(specTokenRefs[token] || []).slice(0, 3).map((ref, idx) => (
              <div key={`${token}-${idx}`} className="sbTokenRef" title={ref}>{ref}</div>
            ))}
            {(specTokenRefs[token] || []).length === 0 ? <div className="sbTokenRef">No direct spec reference found</div> : null}
            {(specTokenRefs[token] || []).length > 3 ? (
              <div className="sbTokenRef">+{(specTokenRefs[token] || []).length - 3} more</div>
            ) : null}
          </div>
        </div>
      ))}
    </div>
  ),
};
