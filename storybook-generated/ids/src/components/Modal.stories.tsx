/* @generated by strict-spec-storybook-gate */
/* component: modal */
import React from "react";
import "../../../../components/ids-theme.css";
import type { Meta, StoryObj } from "@storybook/react";
import { IdsModal } from "../../../../storybook/src/components/IdsModal";
import { Button } from "../../../../storybook/src/components/Button";
import { Tabs } from "../../../../storybook/src/components/Tabs";
import { MODAL_FIGMA_BODY } from "../../../../component-contracts/ids/modal.contract";

const meta: Meta<typeof IdsModal> = {
  title: "Spec Generated/IDS/Modal",
  component: IdsModal,
  parameters: { layout: "centered" },
};

export default meta;
type Story = StoryObj<typeof IdsModal>;

function logEvent(name: string) {
  return () => {
    // eslint-disable-next-line no-console
    console.log(`[IDS Modal event] ${name}`);
  };
}

export const SpecAccurateDesign: Story = {
  name: "Spec Accurate Design",
  render: () => (
    <IdsModal
      trigger={<Button>Open Non-Alerting Dialog</Button>}
      scenario="dialog"
      type="non-alerting"
      size="large"
      defaultOpen={false}
      onClose={logEvent("onClose")}
    >
      <IdsModal.Title>Non-Alerting</IdsModal.Title>
      <IdsModal.Body description={MODAL_FIGMA_BODY} />
      <IdsModal.Footer>
        <Button size="lg" onClick={logEvent("onPrimaryAction")}>
          Close
        </Button>
      </IdsModal.Footer>
    </IdsModal>
  ),
};

export const SinglePageModalUsage: Story = {
  name: "Single-Page Modal Usage",
  render: () => (
    <IdsModal
      trigger={<Button variant="secondary">Open Single-Page Modal</Button>}
      scenario="single-page"
      type="non-alerting"
      size="large"
      defaultOpen={false}
      onClose={logEvent("onClose")}
    >
      <IdsModal.Title>Header</IdsModal.Title>
      <IdsModal.Body description="Single-page modal usage.">
        <div
          style={{
            minHeight: 220,
            border: "1px solid var(--color-border-brand-base)",
            background: "var(--color-background-brand-lighter)",
            padding: 16,
            color: "var(--color-text-neutral)",
          }}
        >
          Single-page usage keeps one continuous content panel without tab/page switching.
        </div>
      </IdsModal.Body>
      <IdsModal.Footer>
        <Button variant="tertiary" size="lg" onClick={logEvent("onTertiaryAction")}>
          Cancel
        </Button>
        <Button size="lg" onClick={logEvent("onPrimaryAction")}>
          Apply
        </Button>
      </IdsModal.Footer>
    </IdsModal>
  ),
};

export const MultiPageModalUsage: Story = {
  name: "Multi-Page Modal Usage",
  render: () => (
    <IdsModal
      trigger={<Button variant="secondary">Open Multi-Page Modal</Button>}
      scenario="multi-page"
      type="non-alerting"
      size="large"
      defaultOpen={false}
      onClose={logEvent("onClose")}
    >
      <IdsModal.Title>Header</IdsModal.Title>
      <IdsModal.Body description={MODAL_FIGMA_BODY}>
        <Tabs
          variant="secondary"
          items={[
            { id: "details", label: "Details", panel: "Page 1 content." },
            { id: "settings", label: "Settings", panel: "Page 2 content." },
            { id: "review", label: "Review", panel: "Page 3 content." },
          ]}
          moreLabel="More"
        />
      </IdsModal.Body>
      <IdsModal.Footer>
        <Button variant="tertiary" size="lg">
          Cancel
        </Button>
        <Button size="lg">Apply</Button>
      </IdsModal.Footer>
    </IdsModal>
  ),
};
