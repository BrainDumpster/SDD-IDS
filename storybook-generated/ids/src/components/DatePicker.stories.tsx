/* @generated by strict-spec-storybook-gate */
/* component: date-picker */
import React, { useState } from "react";
import "../../../../components/ids-theme.css";
import type { Meta, StoryObj } from "@storybook/react";
import { IdsDatePicker } from "../../../../storybook/src/components/IdsDatePicker";

function IdsThemeDecorator(Story: React.ComponentType) {
  React.useEffect(() => {
    document.documentElement.setAttribute("data-design-system", "ids");
    return () => document.documentElement.removeAttribute("data-design-system");
  }, []);
  return <Story />;
}

const meta: Meta<typeof IdsDatePicker> = {
  title: "Spec Generated/IDS/Date Picker",
  component: IdsDatePicker,
  parameters: { layout: "padded" },
  decorators: [IdsThemeDecorator],
};
export default meta;
type Story = StoryObj<typeof IdsDatePicker>;

/* ─── 1. Default ─── */
export const Default: Story = {
  render: () => (
    <div style={{ padding: 24 }}>
      <IdsDatePicker size="large" label="Start date" placeholder="MM-DD-YYYY" />
    </div>
  ),
};

/* ─── 2. Sizes ─── */
export const Sizes: Story = {
  name: "Large & Small",
  render: () => (
    <div style={{ padding: 24, display: "flex", gap: 48, alignItems: "flex-start" }}>
      <div>
        <div style={{ fontSize: 11, fontWeight: 600, textTransform: "uppercase" as const, opacity: 0.6, marginBottom: 8 }}>Large (40 px)</div>
        <IdsDatePicker size="large" label="Date" value={new Date(2026, 0, 18)} />
      </div>
      <div>
        <div style={{ fontSize: 11, fontWeight: 600, textTransform: "uppercase" as const, opacity: 0.6, marginBottom: 8 }}>Small (32 px)</div>
        <IdsDatePicker size="small" label="Date" value={new Date(2026, 0, 18)} />
      </div>
    </div>
  ),
};

/* ─── 3. States matrix ─── */
export const States: Story = {
  name: "Field States",
  render: () => (
    <div style={{ padding: 24, display: "flex", gap: 48, flexWrap: "wrap" as const, alignItems: "flex-start" }}>
      <div>
        <div style={{ fontSize: 11, fontWeight: 600, textTransform: "uppercase" as const, opacity: 0.6, marginBottom: 8 }}>Default</div>
        <IdsDatePicker size="large" label="Date" />
      </div>
      <div>
        <div style={{ fontSize: 11, fontWeight: 600, textTransform: "uppercase" as const, opacity: 0.6, marginBottom: 8 }}>Filled</div>
        <IdsDatePicker size="large" label="Date" value={new Date(2026, 0, 18)} />
      </div>
      <div>
        <div style={{ fontSize: 11, fontWeight: 600, textTransform: "uppercase" as const, opacity: 0.6, marginBottom: 8 }}>Disabled (filled)</div>
        <IdsDatePicker size="large" label="Date" disabled value={new Date(2026, 0, 18)} />
      </div>
      <div>
        <div style={{ fontSize: 11, fontWeight: 600, textTransform: "uppercase" as const, opacity: 0.6, marginBottom: 8 }}>Disabled (empty)</div>
        <IdsDatePicker size="large" label="Date" disabled />
      </div>
      <div>
        <div style={{ fontSize: 11, fontWeight: 600, textTransform: "uppercase" as const, opacity: 0.6, marginBottom: 8 }}>Error</div>
        <IdsDatePicker size="large" label="Date" error errorMessage="Invalid date format" />
      </div>
      <div>
        <div style={{ fontSize: 11, fontWeight: 600, textTransform: "uppercase" as const, opacity: 0.6, marginBottom: 8 }}>Custom format hint</div>
        <IdsDatePicker size="large" label="Date" formatHint="DD/MM/YYYY" dateFormat="DD/MM/YYYY" />
      </div>
    </div>
  ),
};

/* ─── 4. Calendar open — standalone popup border ─── */
export const CalendarOpen: Story = {
  name: "Calendar Open — Standalone Popup Border",
  render: () => (
    <div style={{ padding: 24, paddingBottom: 520 }}>
      <div style={{ maxWidth: 320 }}>
        <IdsDatePicker size="large" label="Date" value={new Date(2026, 0, 18)} forceOpen />
      </div>
    </div>
  ),
};

/* ─── 5. Calendar open — small size ─── */
export const CalendarOpenSmall: Story = {
  name: "Calendar Open — Small",
  render: () => (
    <div style={{ padding: 24, paddingBottom: 520 }}>
      <div style={{ maxWidth: 320 }}>
        <IdsDatePicker size="small" label="Date" value={new Date(2026, 4, 25)} forceOpen />
      </div>
    </div>
  ),
};

/* ─── 6. Interactive ─── */
export const Interactive: Story = {
  render: () => <InteractiveDemo />,
};

function InteractiveDemo() {
  const [value, setValue] = useState<Date | null>(new Date(2026, 0, 18));
  return (
    <div style={{ padding: 24, paddingBottom: 520 }}>
      <div style={{ maxWidth: 320 }}>
        <div style={{ fontSize: 12, opacity: 0.6, marginBottom: 8 }}>
          Selected: {value ? value.toLocaleDateString() : "none"}
        </div>
        <IdsDatePicker
          size="large"
          label="Pick a date"
          value={value}
          onChange={(d) => setValue(d)}
        />
      </div>
    </div>
  );
}

/* ─── 7. Restricted / unavailable dates (Figma 42159:132767) ─── */
export const RestrictedDates: Story = {
  name: "Restricted Dates — Unavailable",
  render: () => {
    const today = new Date();
    const viewMonth = today.getMonth();
    const viewYear = today.getFullYear();
    return (
      <div style={{ padding: 24, paddingBottom: 520 }}>
        <div style={{ maxWidth: 320 }}>
          <IdsDatePicker
            size="large"
            label="Availability"
            value={new Date(viewYear, viewMonth, 15)}
            forceOpen
            disabledDates={[
              new Date(viewYear, viewMonth, 10),
              new Date(viewYear, viewMonth, 11),
              new Date(viewYear, viewMonth, today.getDate()),
              new Date(viewYear, viewMonth, 20),
              new Date(viewYear, viewMonth, 21),
            ]}
          />
        </div>
      </div>
    );
  },
};

/* ─── 8. Restricted dates in range mode ─── */
export const RestrictedDatesInRange: Story = {
  name: "Restricted Dates — Range Mode",
  render: () => <RestrictedRangeDemo />,
};

function RestrictedRangeDemo() {
  const today = new Date();
  const y = today.getFullYear();
  const m = today.getMonth();
  const [start, setStart] = useState<Date | null>(new Date(y, m, 5));
  const [end, setEnd] = useState<Date | null>(new Date(y, m, 25));
  return (
    <div style={{ padding: 24, paddingBottom: 520 }}>
      <div style={{ maxWidth: 320 }}>
        <div style={{ fontSize: 12, opacity: 0.6, marginBottom: 8 }}>
          Restricted dates show gray box, disabled border, and strikethrough (not selectable).
        </div>
        <IdsDatePicker
          size="large"
          label="Date range"
          rangeMode
          rangeStart={start}
          rangeEnd={end}
          onRangeChange={(s, e) => {
            setStart(s);
            setEnd(e);
          }}
          forceOpen
          disabledDates={[
            new Date(y, m, 10),
            new Date(y, m, 11),
            new Date(y, m, 20),
            new Date(y, m, 21),
          ]}
        />
      </div>
    </div>
  );
}

/* ─── 9. Range selection ─── */
export const RangeSelection: Story = {
  render: () => <RangeDemo />,
};

function RangeDemo() {
  const [start, setStart] = useState<Date | null>(null);
  const [end, setEnd] = useState<Date | null>(null);
  return (
    <div style={{ padding: 24, paddingBottom: 520 }}>
      <div style={{ maxWidth: 320 }}>
        <div style={{ fontSize: 12, opacity: 0.6, marginBottom: 8 }}>
          Range: {start ? start.toLocaleDateString() : "—"} → {end ? end.toLocaleDateString() : "—"}
        </div>
        <IdsDatePicker
          size="large"
          label="Date range"
          rangeMode
          rangeStart={start}
          rangeEnd={end}
          onRangeChange={(s, e) => { setStart(s); setEnd(e); }}
          forceOpen
        />
      </div>
    </div>
  );
}
