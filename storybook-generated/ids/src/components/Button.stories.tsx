/* @generated by strict-spec-storybook-gate */
/* component: button */
/* spec_hash: 73bad3dd1215b86ea547b61105e56c3d6bec30037c986f8b20ae9ddc2258a051 */
import type { Meta, StoryObj } from "@storybook/react";
import type { ComponentProps } from "react";
import { IdsButton } from "../../../../storybook/src/components/IdsButton";
import { Icon } from "../../../../storybook/src/components/Icon";

type Variant = "primary" | "secondary" | "tertiary" | "destructive";
type Size = "sm" | "md" | "lg";

const meta: Meta<typeof IdsButton> = {
  title: "Spec Generated/IDS/Button",
  component: IdsButton,
  parameters: { layout: "centered" },
  argTypes: {
    variant: { control: "select", options: ["primary", "secondary", "tertiary", "destructive"] },
    size: { control: "select", options: ["sm", "md", "lg"] },
    disabled: { control: "boolean" },
    loading: { control: "boolean" },
    iconOnly: { control: "boolean" },
    children: { control: "text" },
  },
  args: {
    variant: "primary",
    size: "md",
    disabled: false,
    loading: false,
    iconOnly: false,
    children: "Button",
  },
};

export default meta;
type Story = StoryObj<typeof IdsButton>;

function SimButton(
  props: ComponentProps<typeof IdsButton> & {
    simState?: "default" | "hover" | "press" | "focus-visible";
    simVariant?: Variant;
  },
) {
  const { simState = "default", simVariant, className, ...rest } = props;
  return (
    <IdsButton
      {...rest}
      data-sim-state={simState}
      data-sim-variant={simVariant ?? (rest.variant as Variant | undefined) ?? "primary"}
      className={["sbSimButton", className].filter(Boolean).join(" ")}
    />
  );
}

export const Playground: Story = {
  render: (args) => (
    <IdsButton
      {...args}
      aria-label={args.iconOnly ? "Icon only button" : undefined}
      icon={args.iconOnly || args.iconSlug ? <Icon shapeName="docs-bundle" variant="mask" /> : undefined}
    />
  ),
};

export const Variants: Story = {
  render: () => (
    <div className="sbVariantGrid">
      <IdsButton variant="primary">Primary</IdsButton>
      <IdsButton variant="secondary">Secondary</IdsButton>
      <IdsButton variant="tertiary">Tertiary</IdsButton>
      <IdsButton variant="destructive">Destructive</IdsButton>
    </div>
  ),
};

export const Disabled: Story = {
  render: () => (
    <div className="sbVariantGrid">
      <IdsButton variant="primary" disabled>Primary</IdsButton>
      <IdsButton variant="secondary" disabled>Secondary</IdsButton>
      <IdsButton variant="tertiary" disabled>Tertiary</IdsButton>
      <IdsButton variant="destructive" disabled>Destructive</IdsButton>
    </div>
  ),
};

export const IconWithLabel: Story = {
  args: { size: "md", disabled: false },
  render: (args) => (
    <div className="sbVariantGrid">
      <IdsButton size={args.size} disabled={args.disabled} icon={<Icon shapeName="docs-bundle" variant="mask" />}>Primary</IdsButton>
      <IdsButton size={args.size} disabled={args.disabled} variant="secondary" icon={<Icon shapeName="status-ok-circ-solid" variant="img" />}>Secondary</IdsButton>
      <IdsButton size={args.size} disabled={args.disabled} variant="tertiary" icon={<Icon shapeName="state-progress-circle" variant="img" />}>Tertiary</IdsButton>
    </div>
  ),
};

export const IconOnly: Story = {
  args: { size: "md", disabled: false },
  argTypes: { size: { control: "select", options: ["md", "lg"] } },
  render: (args) => (
    <div className="sbIconOnlyGrid">
      <IdsButton iconOnly size={args.size} disabled={args.disabled} aria-label="Primary icon button" icon={<Icon shapeName="docs-bundle" variant="mask" />} />
      <IdsButton iconOnly size={args.size} disabled={args.disabled} aria-label="Secondary icon button" variant="secondary" icon={<Icon shapeName="status-ok-circ-solid" variant="img" />} />
      <IdsButton iconOnly size={args.size} disabled={args.disabled} aria-label="Tertiary icon button" variant="tertiary" icon={<Icon shapeName="state-progress-circle" variant="img" />} />
    </div>
  ),
};

export const LayoutTokens: Story = {
  render: () => (
    <style>{
      `
      .sbVariantGrid {
        display: grid;
        grid-template-columns: repeat(2, max-content);
        gap: 12px;
      }
      .sbIconOnlyGrid {
        display: grid;
        grid-template-columns: repeat(3, max-content);
        gap: 12px;
      }
      `
    }</style>
  ),
};


export const StateHarness: Story = {
  render: () => (
    <div className="sbGrid">
      <style>{
        `
        .sbGrid {
          display: grid;
          gap: 16px;
        }
        .sbRow {
          display: grid;
          grid-template-columns: 140px repeat(4, max-content);
          gap: 12px;
          align-items: center;
        }
        .sbLabel {
          font-size: 12px;
          opacity: 0.8;
          text-transform: capitalize;
        }
        .sbSimButton[data-sim-state="hover"][data-sim-variant="primary"]:not(:disabled) {
          background: var(--color-background-controls-brand-strong);
        }
        .sbSimButton[data-sim-state="press"][data-sim-variant="primary"]:not(:disabled) {
          background: var(--color-background-controls-brand-stronger);
        }
        .sbSimButton[data-sim-state="hover"][data-sim-variant="secondary"]:not(:disabled) {
          background: var(--color-background-controls-brand-lighter);
        }
        .sbSimButton[data-sim-state="press"][data-sim-variant="secondary"]:not(:disabled) {
          background: var(--color-background-controls-brand-light);
        }
        .sbSimButton[data-sim-state="hover"][data-sim-variant="tertiary"]:not(:disabled),
        .sbSimButton[data-sim-state="press"][data-sim-variant="tertiary"]:not(:disabled) {
          border-color: var(--color-border-brand-base);
        }
        .sbSimButton[data-sim-state="hover"][data-sim-variant="tertiary"]:not(:disabled) {
          background: var(--color-background-controls-brand-lighter);
        }
        .sbSimButton[data-sim-state="press"][data-sim-variant="tertiary"]:not(:disabled) {
          background: var(--color-background-controls-brand-light);
        }
        .sbSimButton[data-sim-state="hover"][data-sim-variant="destructive"]:not(:disabled) {
          background: var(--color-background-alerting-critical-strong);
        }
        .sbSimButton[data-sim-state="press"][data-sim-variant="destructive"]:not(:disabled) {
          background: var(--color-background-alerting-critical-stronger);
        }
        .sbSimButton[data-sim-state="focus-visible"]:not(:disabled) {
          outline: var(--border-width-border-1) solid var(--color-border-brand-base);
          outline-offset: 2px;
        }
        `
      }</style>

      {(["primary", "secondary", "tertiary", "destructive"] as const).map((variant) => (
        <div key={variant} className="sbRow">
          <div className="sbLabel">{variant}</div>
          <SimButton simVariant={variant} simState="default" variant={variant}>Default</SimButton>
          <SimButton simVariant={variant} simState="hover" variant={variant}>Hover</SimButton>
          <SimButton simVariant={variant} simState="press" variant={variant}>Press</SimButton>
          <SimButton simVariant={variant} simState="focus-visible" variant={variant}>Focus</SimButton>
        </div>
      ))}
    </div>
  ),
};

